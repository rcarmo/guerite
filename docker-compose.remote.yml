version: "3.9"

services:
  guerite:
    image: ghcr.io/rcarmo/guerite:latest
    environment:
      DOCKER_HOST: "tcp://remote-docker-host:2376"
      DOCKER_TLS_VERIFY: "1"
      DOCKER_CERT_PATH: "/certs"
      GUERITE_LOG_LEVEL: INFO
    volumes:
      - ./certs:/certs:ro
    restart: unless-stopped
